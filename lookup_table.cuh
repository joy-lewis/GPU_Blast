#pragma once
#include <cstdint>
#include <vector>
#include <stdexcept>





    /*
start = d_offsets[key];
end   = d_offsets[key + 1];
for (uint32_t t = start; t < end; ++t) {
uint32_t qpos = d_positions[t];
...
}
    */
